{
  "name": "research-flow",
  "version": "0.8.1",
  "private": true,
  "description": "A single-project visual workflow editor",
  "type": "module",
  "scripts": { "test": "node --test tests/test_*.mjs" },
  "engines": { "node": ">=22" }
}
